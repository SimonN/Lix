/*
 * other/types.h
 *
 * Typedefs for unsigned char, unsigned int, unsigned long
 *
 * For the Uint32 type I use for platform-independent exchange of
 * 32 bit unsigned integers, see net_t.h.
 *
 */

#pragma once

typedef unsigned char Byte;

typedef unsigned      Uint;
typedef unsigned long Ulng;

typedef int           AlCol; // allegro color
